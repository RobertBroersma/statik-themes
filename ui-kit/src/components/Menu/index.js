import styled from 'styled-components'
import { css } from '@styled-system/css'

export const Menu = styled.ul(
  css({
    listStyle: 'none',
    padding: 0,
    margin: 0,
  }),
)

export const MenuItem = styled.li(css({}))
